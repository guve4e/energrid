# Automation Core

Provider-neutral automation pipeline contracts for Energrid.

The LLM can observe context, server errors, state, and policy decisions, and it
can propose intents/actions. Execution still goes through the mandatory policy
gate before touching physical devices.

